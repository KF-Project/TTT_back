<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kf.ttt.repository.NumRepository">
    <!-- 결과값 입력 -->
    <!-- <update id="numToKor" parameterType="com.kf.ttt.entity.Num">
        UPDATE log SET trans_result = (#{result})
        WHERE user_id = 'kf1';
    </update> -->
    <select id="numToKor" parameterType="com.kf.ttt.entity.Num">
        select * from log
    </select>
</mapper>