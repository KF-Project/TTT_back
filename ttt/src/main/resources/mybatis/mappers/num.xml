<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kf.ttt.resources.mybatis.mappers.num">
    <!-- 결과값 입력 -->
    <insert id="insertTransResult" parameterType="com.kf.ttt.entity.Num">
        <!-- INSERT INTO trans_result (result)
        VALUES (#{result}) -->
    </insert>
</mapper>